<UserControl x:Class="Elixir.Views.ScreenCapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             Background="#1F1F1F">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Title & Device Selection -->
            <RowDefinition Height="Auto"/>
            <!-- Button Panel -->
            <RowDefinition Height="Auto"/>
            <!-- Status Message -->
            <RowDefinition Height="Auto"/>
            <!-- Editing Controls -->
            <RowDefinition Height="*"/>
            <!-- Image Preview -->
        </Grid.RowDefinitions>

        <!-- Title & Device Selection -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="ScreenCap+" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>
            <ComboBox x:Name="DeviceList" Height="30" Width="300" Background="White" Foreground="Black" FontSize="14" HorizontalAlignment="Left" />
        </StackPanel>

        <!-- Button Panel -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
            <Button Content="Refresh" Click="RefreshDevices" Width="100" Height="30" Margin="0,0,10,0"/>
            <Button Content="Capture Screen" Click="CaptureScreen" Width="150" Height="30"/>
        </StackPanel>

        <!-- Status Message -->
        <TextBlock Grid.Row="2" x:Name="StatusMessage" Text="Waiting for action..." FontSize="12" 
                   Foreground="White" Margin="0,10,0,10" TextWrapping="Wrap" HorizontalAlignment="Left"/>

        <!-- Editing Controls -->
        <DockPanel Grid.Row="3" LastChildFill="True" Background="#1F1F1F" Height="50">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" Margin="10,0,0,0" VerticalAlignment="Center">

                <!-- Pen Button (Red) -->
                <Button Click="SetPenMode" Width="40" Height="40" Margin="5,0,5,0" Style="{StaticResource ToolButton}" ToolTip="Draw with Pen">
                    <fa:IconBlock Icon="Pen" Foreground="Red" FontSize="18"/>
                </Button>

                <!-- Color Picker -->
                <Rectangle Width="30" Height="30" Fill="Red" Stroke="White" StrokeThickness="1" Margin="5,0,10,0" MouseDown="OpenColorPicker" ToolTip="Select Pen Color"/>

                <!-- Eraser Button (Orange) -->
                <Button Click="SetEraserMode" Width="40" Height="40" Margin="5,0,5,0" Style="{StaticResource ToolButton}" ToolTip="Erase strokes">
                    <fa:IconBlock Icon="Eraser" Foreground="Orange" FontSize="18"/>
                </Button>

                <!-- Undo Button (Yellow) -->
                <Button Click="UndoLastStroke" Width="40" Height="40" Margin="5,0,5,0" Style="{StaticResource ToolButton}" ToolTip="Undo last stroke">
                    <fa:IconBlock Icon="Undo" Foreground="Yellow" FontSize="18"/>
                </Button>

                <!-- Clear Button (Blue) -->
                <Button Click="ClearCanvas" Width="40" Height="40" Margin="5,0,5,0" Style="{StaticResource ToolButton}" ToolTip="Clear all strokes">
                    <fa:IconBlock Icon="Trash" Foreground="Blue" FontSize="18"/>
                </Button>

                <!-- Save Button (Green) -->
                <Button Click="SaveEditedImage" Width="40" Height="40" Margin="5,0,5,0" Style="{StaticResource ToolButton}" ToolTip="Save edited image">
                    <fa:IconBlock Icon="Save" Foreground="Green" FontSize="18"/>
                </Button>
            </StackPanel>
        </DockPanel>

        <!-- Image & Drawing Canvas -->
        <Border Grid.Row="4" BorderBrush="#444" BorderThickness="1">
            <Grid Background="Blue">
                <Viewbox Stretch="Uniform">
                    <Grid Width="960" Height="540">
                        <!-- Screenshot Image -->
                        <Image x:Name="ScreenshotImage" Width="960" Height="540"
                           Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <!-- InkCanvas for drawing -->
                        <InkCanvas x:Name="DrawingCanvas" Width="960" Height="540" Background="Transparent"/>
                    </Grid>
                </Viewbox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
